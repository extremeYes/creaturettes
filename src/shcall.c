#include <stdio.h>
#include <stdlib.h>
// #include <stdbool.h>

bool sh(char *str, ...) {
    // static Cmd cmd = {0};

    // cmd.count = 0;
    // cmd_append(&cmd, str);
    // return cmd_run_sync(cmd);
}
